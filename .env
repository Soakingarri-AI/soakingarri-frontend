# Leave empty in local dev — Vite proxies /api/* to https://api.soakingarri.com,
# so the browser never makes a cross-origin request and CORS is avoided entirely.
# In production (deployed to *.soakingarri.com) set this to the actual API origin.
VITE_API_BASE_URL=""
